<%= simple_form_for [@dream, @comment] do |f| %>
  <%= f.hidden_field :parent_id, value: parent_id rescue nil %>
  <%= f.input :body, label: 'Add a comment' %>
  <%= link_to "Cancel", dream_path(@dream), class: "btn btn-light" %>
  <%= f.button :submit %>
<% end %>
